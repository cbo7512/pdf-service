services:
  pdf-service:
    build: .
    restart: unless-stopped
    volumes:
      - pdf_tmp:/tmp/pdf_sessions

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    depends_on:
      - pdf-service

volumes:
  pdf_tmp:
